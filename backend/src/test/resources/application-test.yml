# Used by the Testcontainers-backed tests; the container supplies the connection details.
spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

app:
  # Two learners, so tests can prove one cannot see or affect the other.
  learners: tester@example.com=tester:Tester,other@example.com=other:Other
