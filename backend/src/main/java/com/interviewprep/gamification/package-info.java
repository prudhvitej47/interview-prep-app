/**
 * Stars, the weekly streak, freezes and milestones (proposal section G). Owns no tables: everything
 * is worked out from attempts, weekly plans and planned breaks each time it is shown, so it can
 * always be rebuilt and cannot drift from what the learner actually did.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Gamification")
package com.interviewprep.gamification;
