/**
 * Interview evidence: who reported what, where and when, and which topics each question maps to.
 * Justifies why a unit exists and how often its subject recurs.
 *
 * <p>Empty until PR 6 — the V2 migration defines its tables and the loader fills them.
 */
@org.springframework.modulith.ApplicationModule(displayName = "Evidence")
package com.interviewprep.evidence;
